// src/components/Converter.tsx

import FileUploader from './FileUploader';

/**
 * Simple wrapper around FileUploader used on the marketing/landing page.
 */
const Converter = () => {
  return <FileUploader />;
};

export default Converter;
